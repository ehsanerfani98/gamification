# 🆘 راهنمای کار با پلتفرم گیمیفیکیشن SaaS

این سند راهنمای کامل **راه‌اندازی** و **کار روزمره** با برنامه است — برای سه نقش:
**مدیر سایت (Admin)**، **فروشگاه‌دار (Merchant)** و **مشتری فروشگاه (Customer)**.

> اگر فقط دنبال یک موضوع خاص هستید:
> [پیش‌نیازها](#۱-پیش‌نیازها) · [راه‌اندازی محلی](#۲-راه‌اندازی-محلی) · [پیکربندی](#۳-پیکربندی-env) ·
> [دموی آماده](#۴-دموی-آماده-یک-دقیقه‌ای) · [پنل فروشگاه‌دار](#۵-پنل-فروشگاه‌دار-کار-با-برنامه) ·
> [PWA مشتری](#۶-سمت-مشتری-pwa-بازی-و-جایزه) · [سندباکس و بتا](#۷-حالت-سندباکس-و-بتای-۵-فروشگاه) ·
> [استقرار روی سرور](#۸-استقرار-روی-سرور-کلیدهای-واقعی) · [مرجع دستورها](#۹-مرجع-دستورهای-artisan) ·
> [خلاصه API](#۱۰-خلاصه-api) · [رفع اشکال](#۱۱-خطاهای-رایج-و-رفع-مشکل)

---

## ۱) پیش‌نیازها

| ابزار | نسخه | توضیح |
|-------|------|-------|
| PHP | **8.4+** | با اکستنشن‌های `sqlite3`، `mbstring`، `curl`، `zip`، `bcmath`، `openssl`، `dom` |
| Composer | 2.x | نصب وابستگی‌های بک‌اند |
| Node.js | 20+ | ساخت فرانت‌اند (Vite) — فقط برای توسعه یا استقرار |
| دیتابیس | SQLite (پیش‌فرض) | بدون نصب؛ آماده مهاجرت به PostgreSQL بدون تغییر کد دامنه |
| Git | — | دریافت کد |

> **نکته ویندوز:** ساده‌ترین راه استفاده از WSL2 یا Laravel Herd است؛ در لینوکس/مک کافی است PHP 8.4 و اکستنشن‌های بالا نصب باشند.

---

## ۲) راه‌اندازی محلی

### گام ۱ — دریافت کد و نصب وابستگی‌ها

```bash
git clone https://github.com/ehsanerfani98/gamification.git
cd gamification

composer install
npm ci
```

### گام ۲ — پیکربندی اولیه

```bash
cp .env.example .env
php artisan key:generate

# ساخت فایل دیتابیس SQLite (خالی)
touch database/database.sqlite
```

### گام ۳ — مهاجرت و داده اولیه

```bash
php artisan migrate --seed
```

این دستور سه **Plan** پیش‌فرض (رایگان / پایه / حرفه‌ای) و **۱۰ بازی MVP** را در دیتابیس ثبت می‌کند.

### گام ۴ — ساخت فرانت‌اند

```bash
npm run build
```

### گام ۵ — اجرا

```bash
php artisan serve
# → http://localhost:8000  (ریدایرکت خودکار به /panel)
```

ورودی‌های اصلی برنامه:

| مسیر | نقش | توضیح |
|------|-----|-------|
| `/panel` | فروشگاه‌دار / Admin | پنل مدیریت (SPA با hash-router) |
| `/c/{slug}` | مشتری | PWA بازی کمپین (موبایل‌اول، RTL) |
| `/api/v1/...` | API | نسخه‌دار، پاسخ JSON استاندارد |

### Worker صف (فقط اگر پیامک واقعی فعال باشد)

در حالت توسعه (`SMS_CHANNEL=log` یا سندباکس پیامک) نیازی به Worker نیست.
وقتی درایور پیامک واقعی (IPPanel) فعال است، ارسال SMS از طریق صف انجام می‌شود:

```bash
php artisan queue:work --tries=3 --backoff=5
```

---

## ۳) پیکربندی (env)

همه تنظیمات حساس در `.env` است و **هیچ‌وقت** در گیت کامیت نمی‌شود.

### پیامک OTP

| متغیر | مقادیر | رفتار |
|-------|--------|-------|
| `SMS_CHANNEL` | `log` (پیش‌فرض) | کد OTP در `storage/logs/laravel.log` ثبت می‌شود — مناسب توسعه |
| | `ippanel` | ارسال واقعی SMS با IPPanel — نیازمند Worker صف |
| `IPANEL_API_KEY` | کلید API پنل IPPanel | فقط برای `ippanel` |
| `IPANEL_ORIGINATOR` | شماره خط ارسال | فقط برای `ippanel` |
| `IPANEL_BASE_URL` | `https://api2.ippanel.com` | قابل پیکربندی برای تست |

### دروازه پرداخت

| متغیر | مقادیر | رفتار |
|-------|--------|-------|
| `PAYMENT_GATEWAY` | `fake` (پیش‌فرض) | پرداخت آزمایشی بدون دروازه — مناسب تست‌ها |
| | `zarinpal` | پرداخت واقعی ZarinPal API v4 |
| `ZARINPAL_MERCHANT_ID` | شناسه پذیرندگی | فقط برای `zarinpal` |
| `ZARINPAL_BASE_URL` | `https://payment.zarinpal.com` | درگاه پرداخت |
| `ZARINPAL_TOMAN_TO_RIAL` | `true` (پیش‌فرض) | مبالغ سیستم تومان است؛ ZarinPal ریال می‌پذیرد (×۱۰) |
| `ZARINPAL_CALLBACK_URL` | خالی | خالی → `/api/v1/payments/zarinpal/callback` روی `APP_URL` |

### OTP و امنیت

| متغیر | پیش‌فرض | توضیح |
|-------|---------|-------|
| `OTP_TTL_MINUTES` | `5` | عمر کد یک‌بارمصرف |
| `OTP_MAX_ATTEMPTS` | `5` | سقف تلاش اشتباه برای هر کد |
| `GAME_RESULT_HMAC_KEY` | خالی | کلید امضای نتیجه بازی — در production حتماً مقدار تصادفی قوی بگذارید |
| `COUPON_PREFIX` | خالی | پیشوند کد کوپن‌ها (مثلاً حرف برند) |

### نرخ‌مجوزهای OTP (ثابت در کد — فصل ۸ سند معماری)

- **۳ درخواست در ساعت** برای هر شماره موبایل
- **۲۰ درخواست در ساعت** برای هر IP

---

## ۴) دموی آماده (یک‌دقیقه‌ای)

برای دیدن خروجی واقعی بدون ساخت دستی فروشگاه و کمپین:

```bash
php artisan demo:seed
```

این دستور **Idempotent** است (اجرای تکراری داده جدید نمی‌سازد) و می‌سازد:

- فروشگاه نمونه «دمو» (`demo`) با اشتراک فعال
- کمپین منتشرشده **چرخ شانس** در `/c/demo-wheel` با دو جایزه: کوپن ۱۰٪ و ۵۰ امتیاز
- فروشگاه‌دار دمو با شماره `09120000000` (ورود با OTP — در حالت توسعه کد در لاگ است)

ویرایش ادمین وضعیت بتا:

```bash
php artisan beta:status
# وضعیت زنده: سندباکس‌ها، Adminها، فروشگاه‌ها، کمپین‌ها، پرداخت‌ها
# + «گام بعدی پیشنهادی»
```

---

## ۵) پنل فروشگاه‌دار (کار با برنامه)

آدرس: **`/panel`** — ورود فقط با شماره موبایل + کد OTP (بدون رمز عبور).

### ۵-۱) ورود به پنل

1. شماره موبایل را وارد کنید → «ارسال کد».
2. کد ۵ رقمی را از یکی از این مسیرها بگیرید:
   - **سندباکس پیامک روشن:** کد مستقیماً در پاسخ API/صفحه نمایش داده می‌شود (`debug_code`)
   - **درایور `log`:** انتهای فایل `storage/logs/laravel.log`
   - **IPPanel واقعی:** SMS روی موبایل شما
3. کد را وارد کنید → توکن Sanctum صادر و در مرورگر ذخیره می‌شود.

> نرخ ارسال کد: ۳ درخواست در ساعت برای هر شماره. اگر کد نگرفتید، لاگ را ببینید یا کمی صبر کنید.

### ۵-۲) ساخت فروشگاه

- اولین ورود: پنل از شما می‌خواهد **فروشگاه** بسازید (نام + اسلاگ یکتا).
- اسلاگ در لینک عمومی کمپین‌ها ظاهر می‌شود (مثلاً `/c/{campaign-slug}`).
- اگر چند فروشگاه دارید، از هدر پنل با هدر `X-Store-Id` بین آن‌ها جابه‌جا شوید.

### ۵-۳) اشتراک و Plan

- منوی **اشتراک**: مقایسه سه Plan و انتخاب Plan.
- Planها سقف‌ها و بازی‌های مجاز را تعیین می‌کنند (جدول زیر).
- پرداخت Plan پولی:
  - **دروازه واقعی (ZarinPal):** به صفحه زرین‌پال می‌روید، پرداخت می‌کنید، به پنل برمی‌گردید
    (`/panel#/subscription?payment=paid`) و اشتراک خودکار فعال می‌شود.
  - **سندباکس پرداخت (بتا):** صفحه پرداخت آزمایشی داخلی باز می‌شود و انتخاب «پرداخت موفق/ناموفق» دارید —
    کل مسیر فعال‌سازی اشتراک، فاکتور و Audit دقیقاً مثل دروازه واقعی اجرا می‌شود.

| Plan | کمپین فعال | مشارکت/ماه | جایزه/کمپین | بازی‌ها | Referral | بازی روزانه |
|------|-----------|------------|-------------|---------|----------|-------------|
| رایگان | ۱ | ۲۰۰ | ۵ | چرخ، تاس | — | — |
| پایه | ۳ | ۲٬۰۰۰ | ۲۵ | + کارت خراش، جعبه، کارت، بلیط، کوییز | — | — |
| حرفه‌ای | ۱۰ | بی‌نهایت | بی‌نهایت | هر ۱۰ بازی | ✓ | ✓ |

> سقف‌ها **داده‌ای** هستند (features JSON) و Admin می‌تواند بدون انتشار نسخه جدید تغییرشان دهد.

### ۵-۴) کتابخانه بازی

- منوی **بازی‌ها**: ده بازی فعال با نشان Plan («مجاز» / «فقط Plan بالاتر»).
- برای هر بازی می‌توانید **JSON Schema پیکربندی** را ببینید — همان چیزی که فرم ساخت کمپین به‌صورت پویا از آن رندر می‌شود.
- بازی‌ها: چرخ شانس، تاس، کارت خراش، جعبه شانس، کارت شانس، بلیط شانس، کوییز، حافظه، واکنش، جرثقیل.

### ۵-۵) ساخت کمپین (Wizard دو مرحله‌ای)

1. منوی **کمپین‌ها** → «کمپین جدید».
2. **مرحله ۱ — مشخصات:** نام، بازی، تاریخ شروع/پایان، رنگ و تم (Theme).
3. **مرحله ۲ — پیکربندی بازی:** فرم پویا از JSON Schema بازی ساخته می‌شود
   (مثلاً برای چرخ: بخش‌های چرخ با وزن احتمال و جایزه هر بخش).
4. ذخیره → کمپین در حالت **Draft** است (هنوز عمومی نیست).

### ۵-۶) تعریف جایزه‌ها

در صفحه **جزئیات کمپین** → بخش جایزه‌ها، از ۹ نوع جایزه انتخاب کنید:

| نوع | توضیح |
|-----|-------|
| تخفیف درصدی / مبلغ ثابت | کوپن تخفیف برای خرید بعدی |
| ارسال رایگان / محصول رایگان | کوپن ویژه |
| هدیه فیزیکی | با راهنمای تحویل |
| امتیاز | شارژ کیف امتیاز مشتری (Ledger) |
| کوپن فروشگاه / سفارشی | کد دستی خودتان |
| بدون جایزه | برای مدیریت نرخ برد |

برای هر جایزه: **وزن احتمال**، **موجودی** (تعداد قابل صدور) و **سقف برد روزانه** را مشخص کنید.

> **قاعده مهم بودجه:** اگر موجودی یک جایزه تمام شود، موتور جایزه به‌صورت شفاف روی
> باقیمانده انتخاب وزنی می‌کند و در نهایت «بدون جایزه» می‌دهد — سیستم هرگز موجودی منفی نمی‌دهد.

### ۵-۷) انتشار و اشتراک‌گذاری

- در جزئیات کمپین → **انتشار**. سقف «کمپین فعال هم‌زمان» Plan اعمال می‌شود
  (خطای `QUOTA_EXCEEDED` یا `PLAN_GAME_NOT_ALLOWED` اگر مجاز نباشید).
- پس از انتشار، **لینک عمومی** کمپین کپی می‌شود: `https://دامنه-شما/c/{slug}`
- این لینک را در بایوی اینستاگرام، استوری یا کپشن بگذارید.

### ۵-۸) ثبت استفاده کوپن (حلقه ROI)

وقتی مشتری کوپن را در فروشگاه شما استفاده کرد، در پنل
(**کوپن‌ها** → «ثبت استفاده») کد را وارد کنید. این کار:

- کوپن را تک‌مصرف می‌کند (استفاده تکراری رد می‌شود)
- گام **Redeem** قیف آنالیتیکس را پر می‌کند → ROI کمپین قابل اندازه‌گیری است

### ۵-۹) آنالیتیکس کمپین

در جزئیات کمپین → **آنالیتیکس**:

- **قیف ۵ گامی:** View → Enter → Play → Win → Redeem + نرخ تبدیل هر گام
- مشتری یکتا و سری زمانی ۳۰ روزه
- تجمیع شبانه خودکار (جاب `analytics:aggregate` ساعت ۰۳:۰۰)

---

## ۶) سمت مشتری (PWA بازی و جایزه)

مشتری فقط با لینک کمپین کار می‌کند — **بدون نصب اپ و بدون ثبت‌نام سایت**:

1. **ورود:** لینک `/c/{slug}` (مثلاً از بایو اینستاگرام) → شماره موبایل + کد OTP
   - توکن مشتری **به‌ازای هر کمپین** جداگانه در دستگاه ذخیره می‌شود (جلوگیری از نشت بین فروشگاه‌ها)
   - اگر فروشگاه Referral فعال کرده باشد (Plan حرفه‌ای)، فیلد اختیاری «کد دعوت» وجود دارد —
     کد نامعتبر ورود را نمی‌شکند
2. **بازی:** دکمه شروع → بازی (چرخ، تاس، خراش، ...) → نتیجه با انیمیشن
   - نتیجه **فقط سمت سرور** تولید می‌شود؛ دستکاری ورودی کلاینت هیچ اثری ندارد
   - قوانین مشارکت (روزی یک‌بار، سقف کل، فقط مشتری جدید) خودکار اعمال می‌شود
3. **نتیجه:** Overlay برد → کد کوپن (با دکمه کپی) یا امتیاز یا هدیه؛ در صورت بی‌جایزه، پیام محترمانه
4. **کیف جایزه‌ها:** دکمه «جایزه‌های من» → کدها، وضعیت استفاده و موجودی امتیاز
5. **نگهداشت (Plan حرفه‌ای):**
   - **چک‌این روزانه:** `+۱۰` امتیاز پایه + پاداش زنجیره ۷/۱۴/۳۰ روز (۵۰/۱۰۰/۲۰۰)
   - **دعوت دوستان:** هر مشتری یک کد دعوت اختصاصی دارد؛ جایزه پله‌ای ۱/۳/۵ دعوت از Ledger
6. **PWA:** روی موبایل «Add to Home Screen» → مانند اپ اجرا می‌شود (آفلاین فقط شل بازی؛ API هرگز کش نمی‌شود)

---

## ۷) حالت سندباکس و بتای ۵ فروشگاه

هدف: اجرای بتا **بدون کلید واقعی** IPPanel/ZarinPal — کلیدها بعداً خودتان روی سرور می‌گذارید.

### ۷-۱) ارتقای کاربر به Admin (یک‌بار)

```bash
php artisan admin:promote 09xxxxxxxxx    # شماره‌ای که با آن وارد پنل می‌شوید
```

سپس **خارج و دوباره وارد پنل شوید** تا منوی Admin ظاهر شود. این دستور Audit هم ثبت می‌کند.

### ۷-۲) تنظیمات سایت (منوی فقط-Admin در پنل)

در پنل → **تنظیمات سایت** دو کلید دارید:

| کلید | روشن | خاموش |
|------|------|-------|
| 📱 **سندباکس پیامک** | هیچ SMS واقعی ارسال نمی‌شود؛ کد OTP در پاسخ API برمی‌گردد (`debug_code`) و لاگ می‌شود — ورود پنل و مشتری کاملاً تست‌پذیر است | ارسال واقعی با درایور env (`log` یا `ippanel`) |
| 💳 **سندباکس پرداخت** | به‌جای ZarinPal، صفحه پرداخت آزمایشی داخلی `/payments/sandbox/{id}` باز می‌شود؛ کاربر «موفق/ناموفق» انتخاب می‌کند و همان callback → verify → فعال‌سازی اشتراک → فاکتور → Audit اجرا می‌شود | دروازه env (`fake` یا `zarinpal`) |

نکته‌های امنیتی:

- این Endpointها فقط با توکن دارای ability «admin» پاسخ می‌دهند (Merchant → 403، مهمان → 401)
- هر تغییر تنظیمات در Audit با رخداد `site.settings_updated` ثبت می‌شود
- پرداخت‌های سندباکس با فلگ `payments.meta.sandbox` تفکیک می‌شوند
- این دو کلید **مقدم بر env** عمل می‌کنند — برای گذار به کلیدهای واقعی فقط کلیدها را خاموش کنید

### ۷-۳) جریان پیشنهادی بتا

1. `demo:seed` → دموی آماده برای ارائه به فروشگاه‌ها
2. `beta:status` → وضعیت زنده بتا + گام بعدی پیشنهادی
3. Onboarding هر فروشگاه طبق **[docs/BETA_ONBOARDING.md](docs/BETA_ONBOARDING.md)**
4. جمع‌آوری بازخورد با **[docs/BETA_FEEDBACK_TEMPLATE.md](docs/BETA_FEEDBACK_TEMPLATE.md)**
5. پایان بتا → کلیدهای واقعی در env (بخش ۸)

---

## ۸) استقرار روی سرور (کلیدهای واقعی)

### ۸-۱) نصب

```bash
# ۱) کد و وابستگی‌ها
composer install --no-dev --optimize-autoloader
npm ci && npm run build

# ۲) پیکربندی
cp .env.example .env && php artisan key:generate --force
#  APP_ENV=production ، APP_DEBUG=false ، APP_URL=https://دامنه-شما

# ۳) دیتابیس
touch database/database.sqlite
php artisan migrate --force

# ۴) بهینه‌سازی
php artisan config:cache && php artisan route:cache && php artisan event:cache
```

### ۸-۲) کلیدهای واقعی (بعد از بتا — بدون تغییر کد)

```env
# پیامک واقعی — IPPanel
SMS_CHANNEL=ippanel
IPANEL_API_KEY=کلید-شما
IPANEL_ORIGINATOR=شماره-خط-ارسال

# پرداخت واقعی — ZarinPal
PAYMENT_GATEWAY=zarinpal
ZARINPAL_MERCHANT_ID=شناسه-پذیرندگی

# امضای نتیجه بازی (اجباری در production)
GAME_RESULT_HMAC_KEY=یک-رشته-تصادفی-طولانی
```

و در پنل (تنظیمات سایت) **هر دو سندباکس را خاموش کنید**.

### ۸-۳) سرویس‌های پس‌زمینه

```bash
# Worker صف — برای ارسال SMS (Supervisor با auto-restart)
php artisan queue:work --tries=3 --backoff=5

# Cron — یک خط، همه زمان‌بندی‌ها:
* * * * * php /path/to/artisan schedule:run
```

زمان‌بندی‌های خودکار:

| جاب | زمان | کار |
|-----|------|-----|
| `sessions:expire` | هر ۵ دقیقه | بستن Sessionهای رهاشده بازی |
| `campaigns:close-expired` | هر ۱۰ دقیقه | بستن کمپین‌های منقضی |
| `analytics:aggregate` | ۰۳:۰۰ | تجمیع قیف + پاک‌سازی رخدادهای >۹۰ روز |
| `database:backup` | ۰۴:۰۰ | بکاپ یکپارچه SQLite (VACUUM INTO، نگه‌داری ۱۴ نسخه) |

### ۸-۴) چک‌لیست امنیتی production

- [ ] `APP_DEBUG=false` و `APP_ENV=production`
- [ ] `GAME_RESULT_HMAC_KEY` تنظیم شده
- [ ] HTTPS فعال (callback زرین‌پال و توکن‌ها به آن وابسته‌اند)
- [ ] Worker صف و cron فعال
- [ ] سندباکس‌های پنل خاموش
- [ ] `php artisan beta:status` همه سبز

---

## ۹) مرجع دستورهای artisan

| دستور | کار |
|-------|-----|
| `php artisan migrate --seed` | مهاجرت + Planها و بازی‌ها |
| `php artisan demo:seed` | دموی Idempotent (`/c/demo-wheel`) |
| `php artisan beta:status` | وضعیت زنده بتا + گام بعدی |
| `php artisan admin:promote {phone}` | ارتقای کاربر پنل به Admin |
| `php artisan database:backup` | بکاپ SQLite بدون توقف (نگه‌داری ۱۴ نسخه) |
| `php artisan analytics:aggregate` | تجمیع دستی قیف (خودکار ۰۳:۰۰) |
| `php artisan sessions:expire` | انقضای Sessionهای رهاشده (خودکار هر ۵ دقیقه) |
| `php artisan campaigns:close-expired` | بستن کمپین‌های منقضی (خودکار هر ۱۰ دقیقه) |
| `php artisan queue:work` | Worker صف (SMS واقعی) |
| `php artisan test` | کل Suite (۱۶۴ تست / ۲٬۷۵۱ assertion) |
| `node scripts/perf/bundle-budget.mjs` | بررسی بودجه JS < 150KB |

---

## ۱۰) خلاصه API

پیشوند همه مسیرها `/api/v1`. پاسخ استاندارد: موفق `data` / خطا `error{code,message,fields}`.
احراز هویت: هدر `Authorization: Bearer {token}` + هدر `X-Store-Id` برای مسیرهای فروشگاه‌دار.

### احراز هویت
| متد | مسیر | توضیح |
|-----|------|-------|
| POST | `/auth/otp/request` | درخواست کد OTP (پنل) |
| POST | `/auth/otp/verify` | تأیید کد → توکن |
| GET | `/auth/me` | کاربر جاری و نقش |

### فروشگاه و اشتراک (Merchant)
| متد | مسیر | توضیح |
|-----|------|-------|
| GET/POST | `/stores` | فهرست / ساخت فروشگاه |
| GET | `/plans` | Planهای قابل خرید |
| GET/POST | `/subscriptions` | اشتراک فعال / درخواست اشتراک |
| GET | `/games` · `/games/{code}/config-schema` | کتابخانه بازی و Schema پویا |

### کمپین، جایزه و گزارش (Merchant)
| متد | مسیر | توضیح |
|-----|------|-------|
| GET/POST | `/campaigns` | فهرست / ساخت کمپین |
| GET/PATCH | `/campaigns/{id}` | جزئیات / ویرایش |
| POST | `/campaigns/{id}/publish` | انتشار |
| GET/POST | `/campaigns/{id}/rewards` | جایزه‌های کمپین |
| GET | `/campaigns/{id}/analytics` | قیف و سری زمانی |
| POST | `/coupons/redeem` | ثبت استفاده کوپن (ROI) |

### مشتری (PWA)
| متد | مسیر | توضیح |
|-----|------|-------|
| GET | `/c/{slug}` | کمپین عمومی (config امن بدون وزن/کلید) |
| POST | `/c/{slug}/otp` · `/c/{slug}/enter` | ورود مشتری |
| POST | `/play/sessions` | شروع Session (هدر Idempotency-Key) |
| POST | `/play/sessions/{token}/action` | اکشن بازی → نتیجه امضاشده HMAC |
| GET | `/me/rewards` | کیف جایزه‌ها و امتیاز |
| POST | `/daily/checkin` | چک‌این روزانه |
| POST | `/referrals/apply` | اعمال کد دعوت |

### پرداخت و ادمین
| متد | مسیر | توضیح |
|-----|------|-------|
| GET | `/payments/zarinpal/callback` | بازگشت مرورگر از دروازه |
| GET/PATCH | `/site-settings` | تنظیمات سایت + سندباکس‌ها (**فقط Admin**) |

---

## ۱۱) خطاهای رایج و رفع مشکل

| علامت | علت | راه‌حل |
|-------|-----|--------|
| کد OTP نمی‌آید | درایور `log` فعال است | `storage/logs/laravel.log` را ببینید، یا سندباکس پیامک را از تنظیمات سایت روشن کنید |
| «تعداد درخواست بیش از حد» | سقف ۳/ساعت هر شماره | صبر کنید یا از شماره دیگر؛ سقف IP: ۲۰/ساعت |
| کد منقضی/نامعتبر | ۵ دقیقه عمر کد، سقف ۵ تلاش | کد جدید بگیرید |
| ورود موفق ولی منوی Admin نیست | نقش Admin اعمال نشده | `admin:promote` و سپس خارج/ورود مجدد |
| 403 روی تنظیمات سایت | توکن Admin نیست | فقط توکن Admin به `/site-settings` دسترسی دارد |
| انتشار کمپین: `QUOTA_EXCEEDED` | سقف «کمپین فعال هم‌زمان» Plan پر است | کمپین دیگری را ببندید/منقضی کنید یا Plan را ارتقا دهید |
| انتشار کمپین: `PLAN_GAME_NOT_ALLOWED` | بازی در Plan شما نیست | بازی جایگزین انتخاب کنید یا Plan را ارتقا دهید |
| `SESSION_CONSUMED` | اکشن دوبار با همان توکن Session | توکن یک‌بارمصرف است؛ Session جدید شروع کنید |
| پرداخت برنگشت | بستن مرورگر در دروازه | اشتراک در بازگشت دوباره با همان Authority فعال می‌شود (Idempotent)؛ برای پیگیری `payments` را ببینید |
| پرداخت failed ولی پول کسر شده | وریفای سمت سرور ناموفق | با پشتیبانی دروازه چک کنید؛ Authority در رکورد Payment هست |
| SMS واقعی نمی‌رود (IPPanel) | Worker صف خاموش | `php artisan queue:work` را (با Supervisor) بالا بیاورید |
| خطای 404 روی منابع فروشگاه دیگر | ایزوله‌سازی Tenant عمدی است | Global Scope اجباری — دسترسی Cross-Store همیشه 404 |
| صفحه سفید پس از `npm run build` | کش مرورگر/میکس | کش را پاک کنید؛ مطمئن شوید `npm run build` بعد از آخرین pull اجرا شده |
| دیتابیس قفل (SQLite) | هم‌زمانی شدید | `DB_BUSY_TIMEOUT` را بالا ببرید؛ WAL پیش‌فرض فعال است |

---

## ۱۲) تست‌ها و CI

```bash
php artisan test                                   # کل Suite
php artisan test --filter=OtpAuth                  # احراز هویت
php artisan test --filter=WheelProbabilityTest     # توزیع احتمال (۱۰٬۰۰۰ اجرا)
php artisan test --filter=SecurityMatrixTest       # ماتریس ۷ حمله امنیتی
php artisan test --filter=ZarinpalPaymentFlow      # جریان پرداخت E2E
```

هر push به GitHub در Actions اجرا می‌شود: **Pint (استایل) + کل Suite + جاب بودجه JS + Lighthouse**.
قبل از push، حتماً:

```bash
./vendor/bin/pint --dirty     # فرمت کد
php artisan test              # سبز بودن Suite
```

---

## ۱۳) لینک‌های مرتبط

- **README.md** — معرفی معماری، سه ستون موتور، ساختار کد
- **TASKS.md** — وضعیت لحظه‌ای اسپرینت‌ها و تسک‌ها
- **docs/BETA_ONBOARDING.md** — راهنمای گام‌به‌گام Onboarding فروشگاه‌های بتا
- **docs/BETA_FEEDBACK_TEMPLATE.md** — فرم بازخورد پایلوت
- سند معماری مصوب: `Gamification_SaaS_Architecture_Design_FA.docx`
