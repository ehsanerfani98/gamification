<?php

// پیکربندی مرکزی پلتفرم گیمیفیکیشن — تنظیمات ضدتقلب و سقف‌های امنیتی (فصل‌های ۵، ۶ و ۸ سند معماری)
return [

    /*
    |--------------------------------------------------------------------------
    | OTP (احراز هویت موبایل + کد یک‌بارمصرف)
    |--------------------------------------------------------------------------
    | دفاع در برابر Brute Force: انقضای کوتاه، سقف تلاش و Rate Limit پلکانی.
    */
    'otp' => [
        'ttl_minutes' => env('OTP_TTL_MINUTES', 5),      // عمر کد
        'max_attempts' => env('OTP_MAX_ATTEMPTS', 5),     // سقف تلاش ناموفق برای هر کد
        'code_length' => 6,
        // سقف درخواست به‌ازای هر شماره — فصل ۸-۳ (۳ در ساعت)
        'request_limit' => ['max' => 3, 'decay_minutes' => 60],
        'ip_limit' => ['max' => 20, 'decay_minutes' => 60],
    ],

    /*
    |--------------------------------------------------------------------------
    | کوپن تخفیف (فصل ۶-۴)
    |--------------------------------------------------------------------------
    | کد یکتا و غیرقابل حدس: الفبای بدون ابهام (بدون 0/O و 1/I) + Prefix برند.
    */
    'coupon' => [
        'length' => 8,
        'prefix' => env('COUPON_PREFIX', ''),
        'alphabet' => 'ABCDEFGHJKLMNPQRSTUVWXYZ23456789',
    ],

    /*
    |--------------------------------------------------------------------------
    | امضای نتیجه بازی (HMAC) — فصل ۵-۲
    |--------------------------------------------------------------------------
    | پاسخ ارسالی به کلاینت قابل اثبات و غیرقابل دستکاری است.
    */
    'signature' => [
        'key' => env('GAME_RESULT_HMAC_KEY') ?: env('APP_KEY'),
    ],

    /*
    |--------------------------------------------------------------------------
    | اشتراک و Plan پیش‌فرض (فصل ۷)
    |--------------------------------------------------------------------------
    */
    'plans' => [
        'default_slug' => 'free',
        // سقف تعداد Store به‌ازای هر Merchant در MVP
        'max_stores_per_merchant' => 5,
    ],

    /*
    |--------------------------------------------------------------------------
    | پرداخت — دروازه تعویض‌پذیر (فصل ۷-۳)
    |--------------------------------------------------------------------------
    */
    'payments' => [
        'gateway' => env('PAYMENT_GATEWAY', 'fake'), // fake | zarinpal | ...
    ],

    /*
    |--------------------------------------------------------------------------
    | Session بازی (فصل ۵-۴)
    |--------------------------------------------------------------------------
    */
    'session' => [
        // عمر توکن یک‌بارمصرف Session — انقضای کوتاه برای دفاع در برابر Replay
        'token_ttl_minutes' => 10,
        // بازی‌های مهارتی: سقف مجاز زمان پاسخ اکشن
        'action_ttl_minutes' => 10,
    ],

    /*
    |--------------------------------------------------------------------------
    | Retention — دعوت دوستان و چک‌این روزانه (فصل ۱۰، Sprint 5)
    |--------------------------------------------------------------------------
    | Referral: با رسیدن تعداد دعوت‌های موفق دعوت‌کننده به هر آستانه،
    | امتیاز همان پله یک‌بار از طریق Ledger پرداخت می‌شود.
    | Check-in: امتیاز پایه هر روز + پاداش رسیدن زنجیره به آستانه‌ها.
    */
    'referral' => [
        'tiers' => [1 => 100, 3 => 300, 5 => 500],
    ],

    'checkin' => [
        'base_points' => 10,
        'streak_bonus' => [7 => 50, 14 => 100, 30 => 200],
    ],

    /*
    |--------------------------------------------------------------------------
    | Analytics (فصل ۱۰، Sprint 5)
    |--------------------------------------------------------------------------
    */
    'analytics' => [
        // دوره نگهداری رخدادهای خام — پس از آن در تجمیع شبانه پاک می‌شوند
        'retention_days' => 90,
    ],
];
